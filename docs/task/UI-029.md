# UI-029 Fix light theme selection popover colors

- **status**: completed
- **priority**: P1
- **owner**: Codex
- **createdAt**: 2026-04-23 16:40

## Description

Fix the light-theme styling bug where selection popovers in the UI
still render with a dark background. Acceptance criteria: shared
selection-style popovers used by the admin and gateway forms follow the
active light or dark theme correctly, without forcing dark colors in
light mode.

## ActiveForm

Completed the shared popup theme fix so selection-style popovers now
follow the active app theme instead of forcing dark mode.

## Dependencies

- **blocked by**: user approval for the proposed scope
- **blocks**: (none)

## Notes

- 2026-04-23 16:40: Reviewed the reported screenshot and matched it to
  the shared select popup styling used in the web UI.
- 2026-04-23 16:40: Confirmed
  `crates/gated-web/src/shared/components/ui/select.tsx` forces a
  `dark` class on `SelectContent`, causing `bg-popover` and related
  tokens to resolve to dark colors regardless of the current page
  theme.
- 2026-04-23 16:40: Confirmed
  `crates/gated-web/src/shared/components/ui/dropdown-menu.tsx` also
  forces `dark` on popup content and submenu content, which can produce
  the same bug on action menus in light theme.
- 2026-04-23 16:40: Found the same pattern in
  `crates/gated-web/src/shared/components/ui/context-menu.tsx`, so the
  fix should account for shared selection-style popup surfaces instead
  of only one form field.
- 2026-04-23 16:47: Removed forced `dark` classes from the shared
  select, dropdown-menu, and context-menu popup surfaces; also ran
  focused `eslint --fix` on those touched files because they already
  violated the repository's current style rules and blocked targeted
  lint verification.
- 2026-04-23 16:48: Verification passed with `pnpm exec eslint
  --no-ignore src/shared/components/ui/select.tsx
  src/shared/components/ui/dropdown-menu.tsx
  src/shared/components/ui/context-menu.tsx` and `pnpm exec tsc
  --noEmit -p tsconfig.json` in `crates/gated-web`.
