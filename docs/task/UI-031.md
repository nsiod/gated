# UI-031 Improve the global web error page

- **status**: completed
- **priority**: P1
- **owner**: Codex
- **createdAt**: 2026-04-23 22:02

## Description

Replace the default React Router application error screen in the web UI
with a branded global error page that gives users clearer guidance and
recovery actions. Acceptance criteria: runtime route failures such as
lazy-import fetch errors render a custom full-page error state instead
of the framework default page, with actionable navigation/retry
controls and localized copy.

## ActiveForm

Completed the global web error-page upgrade so uncaught route errors
now render a branded recovery screen instead of the stock router page.

## Dependencies

- **blocked by**: user approval for the proposed scope
- **blocks**: (none)

## Notes

- 2026-04-23 22:02: Reviewed the provided screenshot and confirmed it
  is the default React Router application error screen shown for an
  uncaught runtime error: `TypeError: Failed to fetch dynamically
  imported module`.
- 2026-04-23 22:02: Confirmed
  `crates/gated-web/src/app/router.tsx` uses `createBrowserRouter(...)`
  without any `errorElement`, so route lazy-import failures and other
  uncaught route errors fall back to the stock framework page.
- 2026-04-23 22:02: Confirmed the app already has shared UI primitives,
  i18n, theme tokens, and button/card components that can support a
  branded full-page error state without introducing a separate design
  system.
- 2026-04-23 22:07: Added a root-level router `errorElement`, created a
  branded global error page in `crates/gated-web/src/app/`, and added
  localized copy plus focused component coverage for chunk-load and
  generic runtime error rendering.
- 2026-04-23 22:07: Verification passed with `pnpm exec vitest run
  src/app/global-error-page.test.tsx`, `pnpm exec tsc --noEmit -p
  tsconfig.json`, and `pnpm exec eslint
  src/app/router.tsx
  src/app/global-error-page.tsx
  src/app/global-error-page.lib.ts
  src/app/global-error-page.test.tsx` in `crates/gated-web`.
