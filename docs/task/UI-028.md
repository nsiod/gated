# UI-028 Make CLI terminals use a light theme in light mode

- **status**: completed
- **priority**: P1
- **owner**: Codex
- **createdAt**: 2026-04-23 15:55

## Description

Make the gateway CLI terminals use a light xterm theme when the application is in light mode, while keeping the existing dark terminal styling in dark mode. Acceptance criteria: SSH and database CLI terminal surfaces in `/ui` and the integrated workspace use a light terminal palette in light mode without regressing terminal behavior or non-terminal pages.

## ActiveForm

Investigating and proposing a light-mode CLI terminal theme that stays consistent across the gateway terminal entry points.

## Dependencies

- **blocked by**: user approval for the proposed theme scope
- **blocks**: (none)

## Notes

- 2026-04-23 15:55: Investigated `crates/gated-web/src/features/gateway/lib/terminal-sessions.ts` and found xterm is hardcoded to `background: '#1a1a1a'` for the route-based gateway terminal session used by SSH and DB CLI routes.
- 2026-04-23 15:55: Investigated `crates/gated-web/src/features/client/pages/terminal-panel.tsx` and found the integrated workspace terminal also hardcodes a dark xterm palette, so SSH and DB CLI entry points need aligned theme handling.
- 2026-04-23 15:55: Confirmed app theme state is available through `ThemeProviderContext`, but current terminal code does not consume it or react to resolved light/system mode.
- 2026-04-23 15:58: User expanded scope to include SQL CLI mode in addition to SSH.
- 2026-04-23 16:10: Added a shared CLI terminal palette helper and a resolved-theme hook, then applied them to the route-based terminal session and the integrated workspace terminal panel.
- 2026-04-23 16:10: Verification passed with `pnpm exec eslint src/features/gateway/lib/terminal-sessions.ts src/features/gateway/pages/terminal.tsx src/features/client/pages/terminal-panel.tsx src/shared/lib/terminal-theme.ts src/shared/hooks/use-resolved-theme.ts` and `pnpm exec tsc --noEmit -p tsconfig.json` in `crates/gated-web`.
