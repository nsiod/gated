# UI-030 Make DB workspace targets open CLI by default

- **status**: completed
- **priority**: P1
- **owner**: Codex
- **createdAt**: 2026-04-23 21:20

## Description

Change the terminal workspace asset interactions so MySQL and Postgres
targets open the CLI terminal by default instead of the SQL Console.
Acceptance criteria: clicking a DB target in the workspace opens the
CLI tab by default, while explicit SQL Console actions remain
available.

## ActiveForm

Investigating the DB workspace target open flow and preparing the
approved change to make CLI the default for database targets.

## Dependencies

- **blocked by**: user approval for the proposed scope
- **blocks**: (none)

## Notes

- 2026-04-23 21:20: Reviewed the provided screenshot and matched it to
  the client workspace asset context menu in
  `crates/gated-web/src/features/client/components/client-layout.tsx`.
- 2026-04-23 21:20: Confirmed the current default open path goes
  through `openOrFocus(target)` and `forceNewTab(target)`, both of
  which fall back to `defaultModeFor(target.kind)`.
- 2026-04-23 21:20: Confirmed `defaultModeFor()` currently maps
  `MySql` and `Postgres` to `gui`, so clicking a DB asset opens SQL
  Console unless the user explicitly picks `Open Terminal (CLI)` from
  the context menu.
- 2026-04-23 21:24: Updated
  `crates/gated-web/src/features/client/components/client-layout.tsx`
  so the workspace default open mode is CLI for DB assets while the
  explicit SQL Console action remains available.
- 2026-04-23 21:24: Verification passed with `pnpm exec tsc --noEmit
  -p tsconfig.json` and `pnpm exec eslint
  src/features/client/components/client-layout.tsx` in
  `crates/gated-web`.
