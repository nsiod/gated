# UI-027 Improve gateway target list command and action layout

- **status**: completed
- **priority**: P1
- **owner**: Codex
- **createdAt**: 2026-04-23 15:40

## Description

Improve the `/ui` gateway target list so connection commands remain readable and action controls do not crowd or truncate the primary content. Acceptance criteria: the target list presents commands and actions with clearer hierarchy on desktop widths like the reported screenshot, and the layout remains usable on narrower screens.

## ActiveForm

Investigating and proposing a clearer gateway target list layout for commands and actions.

## Dependencies

- **blocked by**: user approval for the proposed UI change
- **blocks**: (none)

## Notes

- 2026-04-23 15:40: Investigated `crates/gated-web/src/features/gateway/pages/target-list.tsx` and confirmed the current row packs the command preview, copy action, and launch buttons into one right-aligned flex row.
- 2026-04-23 15:40: Confirmed the page also inherits `whitespace-nowrap` from the shared table cell primitive, which contributes to cramped rendering unless explicitly overridden at the page level.
- 2026-04-23 15:47: Removed the inline command preview from `/ui` target rows and reordered actions to keep database actions as `Open SQL Console`, `Open Terminal`, `Copy`, while SSH keeps `Open Terminal`, `Copy`.
- 2026-04-23 15:47: Verification passed with `pnpm exec eslint src/features/gateway/pages/target-list.tsx` and `pnpm exec tsc --noEmit -p tsconfig.json` in `crates/gated-web`.
