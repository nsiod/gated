# UI-017 类型徽章与文案 i18n 统一

- **status**: completed
- **priority**: P1
- **owner**: claude-code
- **createdAt**: 2026-04-18 09:30
- **claimedAt**: 2026-04-18 10:50
- **completedAt**: 2026-04-18 10:53

## Completion notes

- Added `common:targetTypes.*` locale keys (en + zh-CN) shared by admin and gateway.
- New `shared/lib/target-types.ts` with `targetTypeClass(kind)` helper centralizing badge colors.
- Updated: `admin/pages/config/targets.tsx`, `target-form.tsx`, `target-group-detail.tsx`, `role-detail.tsx`, `gateway/pages/target-list.tsx`.
- Gateway badges now properly localized and color-coded to match admin.
- Old `admin:targets.types.*` keys left in place for potential external references; safe to remove in a followup cleanup.

## 描述

把目标类型徽章 (`Ssh` / `MySql` / `Postgres` / `Kubernetes` / `WebAdmin` / `Api`) 在所有展示位置统一走 i18n，并按类型差异化配色。

### 验收标准

- Gateway 端 `target-list.tsx` 不再渲染原始枚举字符串。
- `target-group-detail.tsx` 中的 `<Badge>` 走 `t('targets.types.${kind}')`。
- 提供按类型差异化的徽章配色（SSH / DB 类 / Web 类 / API 类）。
- en、zh-CN 两份 locale 文件完整。

## ActiveForm

统一类型徽章 i18n 中

## 依赖

- **blocked by**: (none)
- **blocks**: (none)

## 笔记

已有 i18n key：`admin:targets.types.*`。可考虑下沉到 `common:types.*` 让 gateway 共享，避免 admin namespace 跨页面引用。
