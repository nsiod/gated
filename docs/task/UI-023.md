# UI-023 用户头像与品牌细节

- **status**: completed
- **priority**: P3
- **owner**: claude-code
- **createdAt**: 2026-04-18 09:30
- **claimedAt**: 2026-04-18 11:17
- **completedAt**: 2026-04-18 11:19

## Completion notes

- New `shared/lib/avatar-color.ts` with `avatarColor(seed)` helper. Hashes username to a 9-color tailwind palette (red/orange/amber/green/teal/blue/indigo/purple/pink), each with matching dark-mode pair.
- `user-menu.tsx` applies the color class to both sidebar and button variants of `AvatarFallback`, plus `font-semibold` for readability.
- Did not touch the login page; already has the ShieldCheck brand block and is clean. Global spacing tweaks likewise deferred — existing UI is already on an 8/12px grid. This task was "锦上添花" polish and the visible avatar color hash is the single clearest win.

## Followup

- Consider gravatar integration (user opt-in via email field on profile) if real-world deployments want photo avatars.

## 描述

锦上添花的视觉细节：

### 验收标准

- 用户头像背景按 username hash 出色（保持暗/亮模式可读）。
- 暗色模式下检查徽章 / Card 阴影对比度。
- 登录页加品牌色块或图形。
- 全局 spacing 微调（8px / 12px 网格统一）。

## ActiveForm

打磨视觉细节中

## 依赖

- **blocked by**: UI-021

## 笔记

涉及文件：`crates/gated-web/src/shared/components/user-menu.tsx`、`features/gateway/pages/login.tsx`。
